{
    "commands":[
        {
            "name": "help",
            "regex": ["help$"]
        },
        {
            "name": "get_block",
            "regex": ["get_block [a-z0-9]+$"]
        },
        {
            "name": "send",
            "regex": ["send [0-9]+ [a-z0-9]+$"]
        },
        {
            "name": "balance",
            "regex": ["balance [a-z0-9]+$"]
        }
    ]
}